package io.github.freewebmovement.zz

import android.content.Context
import androidx.test.core.app.ApplicationProvider
import io.github.freewebmovement.zz.persistence.Preference
import org.junit.Test
import org.junit.runner.RunWith
import org.mockito.Mockito.mock
import org.robolectric.RobolectricTestRunner


@RunWith(RobolectricTestRunner::class)
class KeyPairUnitTest {
    @Test
    fun should_test_key_pair() {
//        val context: Context = mock(Context::class.java)
//        val preference = Preference(context)
//        val kp = KeyPair(preference)
//        val kp1 = KeyPair(preference)
//        assert(kp.privateKey == kp1.privateKey)
//        assert(kp.publicKey == kp1.publicKey)
    }
}